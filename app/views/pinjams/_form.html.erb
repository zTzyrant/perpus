<%= form_with(model: pinjam, local: true) do |form| %>
  <% if pinjam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pinjam.errors.count, "error") %> prohibited this pinjam from being saved:</h2>

      <ul>
        <% pinjam.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
      <%= form.label :nama %>
      <%= form.text_field :nama, class: 'form-control' %>
    </div>
    <div class="field">
      <%= form.label :phone %>
      <%= form.text_field :phone, class: 'form-control' %>
    </div>
    <div class="field">
      <%= form.label :alamat %>
      <%= form.text_field :alamat, class: 'form-control' %>
    </div>
    <div class="field">
      <%= form.label :book_id %>
      <%= form.select "book_id", options_for_select({ "The Hamlet" => "1", "The Tempest" => "2"}) %>
    </div>
    <div class="actions">
      <%= form.submit 'Submit', :class => 'btn btn-link bg-primary mt-3 text-light btn-block' %>        
    </div>
    <div class="field">
      <%= link_to 'Back',books_path, class: 'btn btn-link bg-danger mt-3 text-light btn-block' %>
    </div>
  <% end %>
